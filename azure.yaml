# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: image-converter
metadata:
  template: image-converter@0.0.1
hooks:
  postprovision:
    shell: pwsh
    run: ./scripts/deploy-app.ps1
